{
  "name": "sdo-dashboard-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Unified monitoring and account management dashboard for the Schools Division of Sipalay City Railway systems",
  "main": "src/server.js",
  "type": "commonjs",
  "license": "ISC",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/config.js && node --check src/db/pools.js && node --check src/db/dashboard.js && node --check src/services/stats.js && node --check src/services/collectors.js && node --check src/routes/api.js && node --check src/middleware/auth.js && node --check public/app.js && node --check public/users.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "connect-pg-simple": "^10.0.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.1",
    "express-session": "^1.19.0",
    "helmet": "^8.3.0",
    "mysql2": "^3.23.2",
    "pg": "^8.22.0"
  }
}